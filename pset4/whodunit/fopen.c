#include<cs50.h>
#include<ctype.h>
#include<stdlib.h>
#include<stdio.h>
int main(void)
{
   FILE *input;
  char get_char;
   input = fopen("large.bmp", "rb");
    while((get_char=fgetc(input))!= EOF)
    {
      return 0;
    }
fclose(input);
}